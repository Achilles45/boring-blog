import styles from "./banner.module.scss"

const Banner = () => {
  return (
    <div className={ styles['banner'] }>
        Welcome to the banner section of the app
    </div>
  )
}

export default Banner